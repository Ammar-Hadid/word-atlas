const Logo = ({ className = "" }) => {
    return <p className={`font-display text-h4 text-primary ${className}`}>Word
        <span className="font-medium"> Atlas</span>
    </p>
}

export default Logo;